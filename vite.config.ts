// TanStack Start & Vite configuration
// The following plugins are managed by the project configuration:
// - tanstackStart, viteReact, tailwindcss, tsConfigPaths, cloudflare
// - componentTagger, VITE_* env injection, @ path alias
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig();
